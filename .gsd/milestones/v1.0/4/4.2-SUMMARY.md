# Plan 4.2 Summary: Pipeline Integration and Parking

- Added `Park(src, root string)` logic to the Mover to gracefully isolate uncertain matches in `.unsorted/`.
- Embedded `Mover` inside `pipeline.Pipeline` struct for orchestrator execution.
- Added execution logic to `Process()` to handle successful matches and fallbacks directly onto the filesystem.
