---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Pipeline Integration and Parking

## Objective
Integrate the Mover into the pipeline orchestrator so decided files are physically relocated or securely parked for manual review.

## Context
- docs/SPEC.md: Tier 3 fallback and parking
- docs/TASKS.md: Task 4.2
- internal/mover/mover.go
- internal/pipeline/pipeline.go

## Tasks

<task type="auto">
  <name>Park Application</name>
  <files>internal/mover/mover.go</files>
  <action>
    Implement `Park(src, rootFolder string) (string, error)` function.
    - Constructs the park destination as `filepath.Join(rootFolder, ".unsorted", filepath.Base(src))`.
    - Leverages the existing atomic `Move` method.
  </action>
  <verify>go test ./internal/mover/...</verify>
  <done>Park properly isolates unconfident files.</done>
</task>

<task type="auto">
  <name>Pipeline Orchestration Updates</name>
  <files>internal/pipeline/pipeline.go</files>
  <action>
    Integrate `Mover` inside `Pipeline.Process`.
    - If `decision.Action` is `moved`, call `Mover.Move(path, decision.Destination)`.
    - If `decision.Action` is `parked`, call `Mover.Park(path, p.cfg.Watch.Folders[0])`. (Assume first folder as root for parking right now).
    - Update `Decision` with the finalized destination path generated.
    - Only call `logDecision` if the move succeeds.
  </action>
  <verify>go test ./internal/pipeline/...</verify>
  <done>Pipeline moves or parks files correctly according to Tier decisions.</done>
</task>

## Success Criteria
- [ ] Tier decisions translate into real atomic filesystem moves.
- [ ] Ambiguous files correctly end up in an `.unsorted` directory.
- [ ] Pipeline logging correctly logs the final disk location of sorted files.
