---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Folder Indexing (Tier 2 Prep)

## Objective
Implement a filesystem crawler and semantic indexer to create a weighted graph of the user's existing folder structure for fuzzy matching.

## Context
- docs/SPEC.md: Tier 2 section
- docs/ARCHITECTURE.md: internal/graph/graph.go, internal/graph/index.go
- docs/TASKS.md: Task 3.2

## Tasks

<task type="auto">
  <name>Semantic Tokenization of Paths</name>
  <files>internal/graph/graph.go</files>
  <action>
    Implement `TokenisePath(folderName string)` logic in internal/graph/graph.go.
    - Split on `-`, `_`, space.
    - Split camelCase (e.g., `CompilerDesign` -> `[compiler, design]`).
    - Lowercase and deduplicate tokens.
  </action>
  <verify>go test ./internal/graph/...</verify>
  <done>Folder names correctly tokensize into semantic keywords.</done>
</task>

<task type="auto">
  <name>Folder Graph Crawler and Indexing</name>
  <files>internal/graph/graph.go</files>
  <action>
    Implement `Crawl(roots []string, ignore []string)` and indexing logic.
    - Iterate through specified root folders.
    - Skip ignored paths (node_modules, .git, etc.) and depths > 6.
    - Persist the folder graph (path, keywords, depth) into the SQLite `folder_index` table.
  </action>
  <verify>go test ./internal/graph/...</verify>
  <done>Folder crawler correctly identifies relevant directories and populates the database.</done>
</task>

## Success Criteria
- [ ] Folder paths are semantic tokenized correctly.
- [ ] Crawler efficiently walks the filesystem and obeys ignore lists.
- [ ] SQLite folder index is correctly populated with the discovered structure.
