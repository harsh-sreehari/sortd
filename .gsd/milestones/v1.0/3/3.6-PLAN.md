---
phase: 3
plan: 2
wave: 3
---

# Plan 3.6: LLM Router (Tier 3)

## Objective
Implement Tier 3 LLM routing, covering complex content-aware matching and image vision strategy for ambiguous sorting tasks.

## Context
- docs/SPEC.md: Tier 3 section
- docs/ARCHITECTURE.md: internal/pipeline/tier3.go
- docs/TASKS.md: Task 3.6

## Tasks

<task type="auto">
  <name>Image Vision Strategy</name>
  <files>internal/peek/image.go, internal/llm/backend.go</files>
  <action>
    Implement base64 image encoding and LLM vision request logic.
    - `Peek(path string)` handles images by encoding to base64.
    - `TagRequest` sends `ImageBytes` for vision-capable models (e.g., LLaVA).
  </action>
  <verify>go test ./internal/llm/...</verify>
  <done>Images are correctly encoded and passed as part of the LLM vision prompt.</done>
</task>

<task type="auto">
  <name>Decision Logic and Routing</name>
  <files>internal/pipeline/tier3.go</files>
  <action>
    Implement routing logic based on the LLM's JSON response in internal/pipeline/tier3.go.
    - If confidence >= threshold: Create `Move` decision (new folder if required).
    - If local folder doesn't exist: Allow creation if `create_folders` is true.
    - If low confidence: `Action: "parked"` (to land in .unsorted).
  </action>
  <verify>go test ./internal/pipeline/...</verify>
  <done>The router correctly applies decision logic from the LLM based on confidence and configuration.</done>
</task>

## Success Criteria
- [ ] LLM correctly processes vision inputs for images.
- [ ] Confident matches are routed with reasoning.
- [ ] Ambiguous files land in `.unsorted/`.
- [ ] Successfully creates new folders when appropriate.
