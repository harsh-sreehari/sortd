# Plan 1.2 Summary: Configuration System

- Created `Config`, `WatchConfig`, `LLMConfig`, and `BehaviourConfig` structs in `internal/config/config.go`.
- Added TOML struct tags matching the requirement.
- Implemented `DefaultConfig()` and `LoadConfig()` with fallback and path expansion.
