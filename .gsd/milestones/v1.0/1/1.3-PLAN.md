---
phase: 1
plan: 3
wave: 1
---

# Plan 1.3: Storage Layer

## Objective
Establish the project's data persistence layer using pure Go SQLite.

## Context
- .gsd/SPEC.md
- docs/ARCHITECTURE.md
- docs/TASKS.md: Task 1.3

## Tasks

<task type="auto">
  <name>SQLite Schema Definition</name>
  <files>internal/store/schema.go</files>
  <action>
    Define SQL strings for the sort_log, folder_index, and affinities tables.
    - Create strings for 'CREATE TABLE IF NOT EXISTS' as specified in docs/TASKS.md.
    - Include a slice of all creation strings for sequential execution.
  </action>
  <verify>cat internal/store/schema.go</verify>
  <done>The schema logic is defined with the correct table structures.</done>
</task>

<task type="auto">
  <name>Store Initialization and Core Logging</name>
  <files>internal/store/store.go</files>
  <action>
    Implement Store initialization and core logging functions.
    - Use `modernc.org/sqlite` for pure Go SQLite interaction.
    - Open(path string) (*Store, error) - opens DB and runs migrations.
    - LogDecision(d Decision) error - inserts an entry into sort_log.
    - RecentLog(n int) ([]LogEntry, error) - retrieves recent logs.
  </action>
  <verify>go test ./internal/store/...</verify>
  <done>The store initializes correctly and can log and retrieve decisions.</done>
</task>

## Success Criteria
- [ ] Database schema is correctly migrated on first run.
- [ ] Decisions can be reliably logged to the SQLite database.
- [ ] Basic retrieval of recent logs works as expected for CLI commands.
