---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Database Schema Migration & Decision Logging

## Objective
Add `original_filename` and `reasoning` columns to the `sort_log` table to support future history search and reasoning display. Fix the current `LogDecision` tag handling to correctly serialise slice data into the database.

## Context
- .gsd/SPEC.md
- internal/store/schema.go
- internal/store/store.go

## Tasks

<task type="auto">
  <name>Schema Updates</name>
  <files>internal/store/schema.go</files>
  <action>
    - Add `original_filename TEXT` and `reasoning TEXT` to the `SortLogSchema` constant.
  </action>
  <verify>grep "original_filename TEXT" internal/store/schema.go</verify>
  <done>Schema strings are updated with the new columns</done>
</task>

<task type="auto">
  <name>Auto-Migration & Store Methods</name>
  <files>internal/store/store.go</files>
  <action>
    - In `store.Open()`, after standard migration loop, add safe `ALTER TABLE` queries to inject `original_filename` and `reasoning` columns to pre-existing databases (ignore the error gracefully if the columns already exist).
    - Update the `Decision` struct to include `OriginalFilename string` and `Reasoning string`.
    - Update `LogEntry` struct to include `OriginalFilename string` and `Reasoning string`.
    - Modify `RecentLog` to select and scan these new columns.
    - Modify `LogDecision` to write the original filename and reasoning.
    - Fix tags handling in `LogDecision`: rather than selecting `d.Tags[0]`, check if the slice is empty. If not, serialize the slice using `json.Marshal(d.Tags)` instead of simple string manipulation.
  </action>
  <verify>go build ./internal/store/...</verify>
  <done>Store compiles and LogDecision appropriately serializes tags into JSON</done>
</task>

## Success Criteria
- [ ] Schema updated supporting `original_filename` and `reasoning`
- [ ] Safe alter table commands exist
- [ ] `LogDecision` saves tags using JSON marshaling
- [ ] `RecentLog` correctly fetches all new fields
