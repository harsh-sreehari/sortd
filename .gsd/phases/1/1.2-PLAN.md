---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Configuration System

## Objective
Implement a robust TOML-based configuration system for sortd with default-first design.

## Context
- .gsd/SPEC.md
- docs/ARCHITECTURE.md
- docs/TASKS.md: Task 1.2

## Tasks

<task type="auto">
  <name>Config Structure and Defaults</name>
  <files>internal/config/config.go</files>
  <action>
    Implement the Config struct in internal/config/config.go.
    - Define fields for [watch], [llm], [behaviour] as specified in TASKS.md.
    - Implement a `DefaultConfig()` function return a pointer to a Config struct with sane defaults.
    - Add a method to handle `~` expansion in paths (using os.UserHomeDir()).
  </action>
  <verify>go test ./internal/config/...</verify>
  <done>Config struct exists, DefaultConfig() returns correct defaults, and path expansion works.</done>
</task>

<task type="auto">
  <name>TOML Loading Logic</name>
  <files>internal/config/config.go</files>
  <action>
    Add logic to LoadConfig(path string) (*Config, error).
    - Use `github.com/BurntSushi/toml` for parsing.
    - Silently fall back to DefaultConfig() if no file exists.
    - Merge existing file with defaults (overwrite provided fields).
  </action>
  <verify>go test ./internal/config/...</verify>
  <done>LoadConfig() correctly reads TOML and merges with defaults.</done>
</task>

## Success Criteria
- [ ] Config struct handles defaults correctly.
- [ ] TOML files are parsed and merged into the config.
- [ ] Path expansion for watched folders and log/DB paths works reliably.
