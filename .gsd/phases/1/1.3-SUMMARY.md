# Plan 1.3 Summary: Storage Layer

- Added `SortLogSchema`, `FolderIndexSchema`, and `AffinitiesSchema` string queries in `internal/store/schema.go`.
- Implemented `Store` type and initialization function `Open()` with schema migration in `internal/store/store.go` using pure Go SQLite `modernc.org/sqlite`.
- Added mock placeholders for `LogDecision`, `RecentLog`, `UnsortedFiles`, and `MarkCorrected`.
