---
phase: 4
plan: 2
wave: 1
---

# Plan 4.2: Tag Analytics

## Objective
Provide a way to view tag distributions across organized files, helping users understand how the LLM or rules are classifying their data.

## Context
- internal/store/store.go
- cmd/sortd/main.go

## Tasks

<task type="auto">
  <name>Implement Tag Aggregation Helper</name>
  <files>internal/store/store.go</files>
  <action>
    - Add `AggregatedTags() ([]TagStat, error)` to the Store.
    - Query `sort_log`, unmarshal JSON tags, and count frequencies.
    - Return a list of tags and counts sorted by popularity.
  </action>
  <verify>grep "func (s *Store) AggregatedTags" internal/store/store.go</verify>
  <done>Store can calculate tag statistics.</done>
</task>

<task type="auto">
  <name>Implement Tags Command</name>
  <files>cmd/sortd/main.go</files>
  <action>
    - Add `tags` command.
    - Display a list (or simple bar chart in text) of common tags and their usage counts.
    - Allow optional filtering by folder root.
  </action>
  <verify>./sortd tags --help</verify>
  <done>Tags command is implemented.</done>
</task>

## Success Criteria
- [ ] `sortd tags` displays counts for all used tags.
- [ ] Tags are sorted by most frequent.
