---
phase: 5
plan: 2
wave: 2
---

# Plan 5.2: Review and Log Commands

## Objective
Implement tools to query the history of decisions and an interactive review command to drain the `.unsorted/` parking directory.

## Context
- docs/SPEC.md: CLI for management
- cmd/sortd/main.go
- internal/store/store.go

## Tasks

<task type="auto">
  <name>Log Retrieval Command</name>
  <files>cmd/sortd/main.go</files>
  <action>
    Implement logic for the `logCmd`.
    - Fetch and display the last `N` (e.g., 20) actions logged to the `store`.
    - Print the filename, destination, and confidence visually as a table.
  </action>
  <verify>go build ./cmd/sortd/...</verify>
  <done>The log command cleanly dumps history to stdout.</done>
</task>

<task type="auto">
  <name>Interactive Review Command</name>
  <files>cmd/sortd/main.go</files>
  <action>
    Implement logic for the `reviewCmd`.
    - Find all files currently inside `.unsorted/`.
    - Prompt the user to manually enter the correct destination folder (with history substitution context if applicable).
    - If accepted, call `mover.Move` to finalize their destination and `store.LogDecision` or `store.MarkCorrected`.
  </action>
  <verify>go build ./cmd/sortd/...</verify>
  <done>The review command lists files and correctly transfers decisions to the file system context.</done>
</task>

## Success Criteria
- [ ] Log history dumps successfully.
- [ ] Users can interactively drain unparked files into final folders.
