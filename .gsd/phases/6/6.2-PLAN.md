---
phase: 6
plan: 2
wave: 2
---

# Plan 6.2: Systemd Unit files and Documentation

## Objective
Finalize systemd user unit provisioning and formal documentation in `README.md`.

## Context
- docs/SPEC.md: systemd user service provisioning.
- README.md (root)
- sortd.service (root)

## Tasks

<task type="auto">
  <name>Create systemd Service Config</name>
  <files>sortd.service</files>
  <action>
    - Create `sortd.service` tailored as a `--user` systemd daemon.
    - Setup `[Unit]`, `[Service]`, `[Install]`.
    - ExecStart maps to `/usr/local/bin/sortd daemon start`.
  </action>
  <verify>cat sortd.service</verify>
  <done>User service is correctly parameterized for user sessions and log tracking.</done>
</task>

<task type="auto">
  <name>Write README.md</name>
  <files>README.md</files>
  <action>
    - Write full README covering Installation (go install or manual build), Usage covering CLI hooks, and the three-tier config setup (`config.toml`).
  </action>
  <verify>cat README.md</verify>
  <done>ReadMe instructs properly on end-to-end functionality.</done>
</task>

## Success Criteria
- [ ] Users possess all materials required for daemon deployment globally.
- [ ] Documentation effectively bridges the daemon concepts to front-end interaction.
