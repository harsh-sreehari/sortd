---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Pipeline Action Normalisation

## Objective
Standardise the `Action` field across the pipeline. Currently, one rule uses "Software/" as an action, which forces brittle string checks in `pipeline.go` and `main.go`. We will move to a standard "moved" action everywhere.

## Context
- internal/pipeline/tier1.go
- internal/pipeline/pipeline.go
- cmd/sortd/main.go

## Tasks

<task type="auto">
  <name>Normalise Tier 1 Rules</name>
  <files>internal/pipeline/tier1.go</files>
  <action>
    - Update the Software rule to explicitly include `Action: "moved"`.
    - Ensure `Destination` remains "Software/".
  </action>
  <verify>grep "Action: \"moved\"" internal/pipeline/tier1.go</verify>
  <done>Tier 1 rules use 'moved' action for software.</done>
</task>

<task type="auto">
  <name>Clean Pipeline & Main Case Logic</name>
  <files>internal/pipeline/pipeline.go, cmd/sortd/main.go</files>
  <action>
    - In `pipeline.go`, remove the check for `decision.Action == "Software/"`. Only check for "moved".
    - In `main.go`'s `runCmd`, update the switch statement to remove the "Software/" case.
  </action>
  <verify>grep -L "Software/\"" internal/pipeline/pipeline.go</verify>
  <done>Brittle "Software/" string checks are removed from execution logic.</done>
</task>

## Success Criteria
- [ ] Software files are still correctly moved to "Software/" folder.
- [ ] Codebase no longer uses the folder name as an action string.
