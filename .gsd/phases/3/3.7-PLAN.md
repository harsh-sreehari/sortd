---
phase: 3
plan: 1
wave: 4
---

# Plan 3.7: Pipeline Orchestrator

## Objective
Implement the top-level orchestration in internal/pipeline/pipeline.go to manage the sequential Tier 1 -> 2 -> 3 flow for every incoming file.

## Context
- docs/SPEC.md: Three-tier pipeline section
- docs/ARCHITECTURE.md: internal/pipeline/pipeline.go
- docs/TASKS.md: Task 3.7

## Tasks

<task type="auto">
  <name>Tier Orchestration Flow</name>
  <files>internal/pipeline/pipeline.go</files>
  <action>
    Implement `Process(path string) Decision` function in internal/pipeline/pipeline.go.
    - Sequentially call Tier 1, then Tier 2, then Tier 3 if no confident match.
    - Capture the outcome from the first tier that returns a confident match.
  </action>
  <verify>go test ./internal/pipeline/...</verify>
  <done>Pipeline correctly orchestrates tiers in the specified order (1 -> 2 -> 3).</done>
</task>

<task type="auto">
  <name>Integration and Logging</name>
  <files>internal/pipeline/pipeline.go</files>
  <action>
    Integrate with the Watcher, Store, and Mover (placeholder).
    - Log every decision to the SQLite `sort_log` via `store.LogDecision`.
    - Handle `Action: "parked"` by creating a symlink or moving into `.unsorted/`.
  </action>
  <verify>go test ./internal/pipeline/...</verify>
  <done>Each sorting decision is robustly logged and the correct filesystem action is identified.</done>
</task>

## Success Criteria
- [ ] Pipeline sequentially tests all tiers.
- [ ] Every decision is logged to the database.
- [ ] Correctly identifies immediate actions (move, park, skip).
