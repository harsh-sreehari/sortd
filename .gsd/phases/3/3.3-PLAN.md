---
phase: 3
plan: 1
wave: 2
---

# Plan 3.3: Fuzzy Matcher (Tier 2)

## Objective
Implement Tier 2 fuzzy matching using Jaccard similarity and affinity boosting to route files based on filename-to-folder structure semantic overlap.

## Context
- docs/SPEC.md: Tier 2 section
- docs/ARCHITECTURE.md: internal/pipeline/tier2.go
- docs/TASKS.md: Task 3.3

## Tasks

<task type="auto">
  <name>Jaccard Similarity Scoring</name>
  <files>internal/pipeline/tier2.go</files>
  <action>
    Implement filename-to-folder overlap scoring in internal/pipeline/tier2.go.
    - Tokenize incoming filename using the same `TokenisePath` logic from Plan 3.2.
    - Compare filename tokens vs. each folder's keywords.
    - Compute Jaccard similarity: `overlap_count / max(len(filename_tokens), len(folder_keywords))`.
  </action>
  <verify>go test ./internal/pipeline/...</verify>
  <done>Filename-to-folder overlap scoring correctly identifies high-similarity matches.</done>
</task>

<task type="auto">
  <name>Affinity Boosting and Threshold Matching</name>
  <files>internal/pipeline/tier2.go</files>
  <action>
    Implement affinity-weighted scoring and matching logic.
    - Boost score if folder node recently received similar files (from `affinities` table).
    - Match if best score >= `cfg.Behaviour.ConfidenceThreshold`.
    - Return `Decision` and `true` for a successful match.
  </action>
  <verify>go test ./internal/pipeline/...</verify>
  <done>Top match is selected correctly based on the combined score and confidence threshold.</done>
</task>

## Success Criteria
- [ ] Filenames like `compiler-mod-1.pdf` match `Academics/Sem6/Compiler Design/`.
- [ ] Confident matches are automatically routed to existing folder structures.
- [ ] Low-confidence matches correctly fall through to the next tier.
