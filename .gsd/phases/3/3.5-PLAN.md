---
phase: 3
plan: 1
wave: 3
---

# Plan 3.5: LLM Backend

## Objective
Implement an LLMBackend interface and provide a concrete LLM Studio implementation for complex content-aware routing.

## Context
- docs/SPEC.md: LLM Backend Interface section
- docs/ARCHITECTURE.md: internal/llm/
- docs/TASKS.md: Task 3.5

## Tasks

<task type="auto">
  <name>LLMBackend Interface and Request/Response</name>
  <files>internal/llm/backend.go</files>
  <action>
    Define the `LLMBackend` interface in internal/llm/backend.go.
    - Define `TagRequest`: `Filename`, `Extension`, `ContentPeek`, `ImageBytes`, `FolderTree`.
    - Define `TagResponse`: `Tags`, `Destination`, `IsNewFolder`, `Confidence`, `Reasoning`.
  </action>
  <verify>go test ./internal/llm/...</verify>
  <done>Interface matches the specification for Tier 3 routing.</done>
</task>

<task type="auto">
  <name>LM Studio Backend Implementation</name>
  <files>internal/llm/lmstudio.go</files>
  <action>
    Implement `LMStudioBackend` in internal/llm/lmstudio.go.
    - Connect to `POST /v1/chat/completions`.
    - Build correctly structured prompt for the LLM.
    - Parse JSON output from the model's response; handle markdown fences.
  </action>
  <verify>go test ./internal/llm/...</verify>
  <done>LM Studio backend correctly formats requests and parses responses into TagResponse.</done>
</task>

## Success Criteria
- [ ] LLMBackend interface is correctly defined.
- [ ] LM Studio backend can connect, prompt, and parse responses.
- [ ] Correctly builds context from FolderTree and ContentPeek for the model.
