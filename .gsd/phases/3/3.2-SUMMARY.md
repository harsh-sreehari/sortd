# Plan 3.2 Summary: Folder Indexing

- Implemented `TokenisePath` function in `internal/graph/graph.go` with camelCase and special char splitting.
- Implemented `Crawl` function using `filepath.Walk` to identify relevant directories.
- Added logic to filter out ignored patterns and hidden folders.
- Integrated folder keywords extraction into the crawling logic.
